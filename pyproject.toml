[project]
name = "healthcare-phoenix-pricer"
version = "0.1.0"
description = "Monte Carlo pricer for the Healthcare Phoenix Autocallable teaching case"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
]
